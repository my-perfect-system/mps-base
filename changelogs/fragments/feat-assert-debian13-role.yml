---
# Fragment for the refactor pass.
# Compiles into changelogs/changelog.yaml at release time.
minor_changes:
  - |
    New shared role `odem.base.assert_debian13` fails fast on non-Debian-13 hosts. Adopted as a meta dependency by every role that requires Debian 13 (odem-os/{package_manager,system_settings,system_tools}, odem-users/{groups,ssh,sudo,users}).

